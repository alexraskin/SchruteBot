{
  "scripts": {
    "dev": "wrangler pages dev --compatibility-date=2023-01-01 ./pages",
    "deploy": "wrangler pages deploy ./pages",
    "pretty": "prettier --write functions/api/*.ts"
  },
  "dependencies": {
    "hono": "^3.9.1",
    "openai": "^4.16.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230914.0",
    "prettier": "^3.0.3",
    "wrangler": "^3.14.0"
  }
}
